/** Spa / pool module — sidebar, headers, defaults */
export const spaSidebarMenuItem = {
  label: "სპა / აუზი",
  icon: "🍺",
} as const;

export const spaBookingsHeader = `${spaSidebarMenuItem.label} — ბუქინგები`;

export const spaSettingsHeader = "სპა / აუზი Setup";
