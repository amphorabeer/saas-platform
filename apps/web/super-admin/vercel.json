{
  "buildCommand": "cd ../../../ && pnpm install && cd apps/web/super-admin && npx prisma generate --schema=./prisma/schema.prisma && pnpm build",
  "installCommand": "echo 'Skipping default install'",
  "framework": "nextjs"
}

